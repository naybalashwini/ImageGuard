"""
Image Tampering Detection and Forensic Restoration System
Project Structure:

forensic_system/
├── requirements.txt          # Dependencies
├── detector.py               # Tampering detection module (TruFor model)
├── restorer.py               # Forensic-level restoration module
├── utils.py                  # Visualization and utility functions
├── app.py                    # Streamlit UI
└── models/                   # Cached models directory (auto-created)

Usage:
    1. Install dependencies: pip install -r requirements.txt
    2. Run the UI: streamlit run app.py
"""
